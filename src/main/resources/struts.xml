<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 6.0//EN"
        "https://struts.apache.org/dtds/struts-6.0.dtd">
<struts>
    <!-- Resolve action instances through Spring so dependency injection is available. -->
    <constant name="struts.objectFactory" value="spring"/>
    <constant name="struts.devMode" value="false"/>

    <package name="demo" namespace="/struts" extends="struts-default">
        <action name="hello" class="strutsHelloAction">
            <result name="success">/WEB-INF/jsp/struts-home.jsp</result>
        </action>
    </package>
</struts>
